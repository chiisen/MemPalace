# Agent Task State

- 🚩 目標：依操作紀錄補齊 Obsidian CLI 安裝功能（無 API Key、純檔案模式）。
- ✅ 進展：已擴充 `scripts/obsidian-cli.sh` 新增 `install` 子命令，會建立 `~/.local/bin/obsidian-note`、補 PATH、可寫入 `OBSIDIAN_VAULT_PATH`；README 已新增 macOS 安裝與使用流程。
- 🚀 下一步：在本機終端執行 `./scripts/obsidian-cli.sh install "<vault-path>"` 後 `source ~/.zshrc`，再以 `obsidian-note search "BAM"` 驗證。
